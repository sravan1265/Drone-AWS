pipeline:
  build:
    image: docker
    commands: 
      - docker build -t test123 .
#      - docker tag sravan1265/Drone-AWS 
      - docker push sravank/vcloudx:vcx-1.0
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock  
        
